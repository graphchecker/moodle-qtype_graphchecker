{
  "name": "Patterns",
  "checks": {
    "has_sub_graph": {
      "name": "Contains given subgraph",
      "description": "Checks if for a given subgraph the nodes with matching labels form have the exact same arcs and markings.",
      "params": [
        {
          "param": "sub_graph",
          "name": "Subgraph",
          "type": "graph"
        }
      ]
    }
  }
}
