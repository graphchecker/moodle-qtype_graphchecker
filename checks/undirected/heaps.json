{
    "name": "Heap properties",
    "checks": {
        "MaxHeap_structure": {
            "name": "Max-heap structure",
            "description": "Check if the given graph is valid for a max-heap (layout + contents)"
        },
        "MinHeap_structure": {
            "name": "Min-heap structure",
            "description": "Check if the given graph is valid for a min-heap (layout + contents)"
        }
    }
}
